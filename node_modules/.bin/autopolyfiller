#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../autopolyfiller/bin/autopolyfiller" "$@"
  ret=$?
else 
  node  "$basedir/../autopolyfiller/bin/autopolyfiller" "$@"
  ret=$?
fi
exit $ret
